---
import { cn } from '@/lib/utils'

interface Props {
  class?: string,
  id?: string
}

const { class: className, id } = Astro.props
---

<div class={cn('mx-auto max-w-[50rem] px-4', className)} id={id}><slot /></div>
